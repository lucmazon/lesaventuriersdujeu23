# Hugo configuration file
title: Les aventuriers du jeu

# import hextra as module
module:
  imports:
  - path: github.com/imfing/hextra

markup:
  # allow raw html
  goldmark:
    renderer:
      unsafe: true

  # enable hextra syntax highlight
  highlight:
    noClasses: false

menu:
  main:
    - name: Facebook
      params:
        icon: facebook
      url: "https://www.facebook.com/profile.php?id=61585309705562"
      weight: 1
    - name: Jeux
      pageRef: /games
      weight: 2
    - identifier: association
      name: L’association
      weight: 3
    - name: Les statuts
      pageRef: /association/status
      parent: association
    - name: Le règlement intérieur
      pageRef: /association/rules
      parent: association
    - name: Contact
      pageRef: /contact
      weight: 4


params:
  navbar:
    displayTitle: true
    displayLogo: false

  footer:
    displayCopyright: false
    displayPoweredBy: true

  editURL:
    enable: false
    base: "https://github.com/imfing/hextra-starter-template/edit/main/content"
